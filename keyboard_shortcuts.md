# 键盘快捷键说明

## 完整快捷键列表

在 matplotlib 可视化窗口中，支持以下快捷键：

| 按键 | 功能 | 说明 |
|------|------|------|
| **s** | 启动自动控制 | 开始发送电机命令，小车开始自主探索 |
| **p** | 强制停止小车 | 立即发送 `CMD-SET 0 0` 并关闭自动控制 |
| **m** | 保存地图和路径 | 保存当前地图为 `map.png`，路径为 `path.csv` |
| **空格** | 暂停/继续可视化 | 暂停或恢复可视化更新（控制循环继续） |

## 详细说明

### 🚀 启动控制 [s]

**功能：** 启动自动控制，小车开始移动

**使用场景：**
- 程序启动后默认处于"仅建图模式"
- 按下 `s` 键后，DWA开始发送速度命令
- 小车开始自主探索未知区域

**状态提示：**
```
[Visualizer] ✅ 自动控制已启动！现在将开始发送电机命令。
```

**窗口标题变化：**
- 启动前：`📍 仅建图模式 (按's'启动)`
- 启动后：`🚗 自动控制中`

**注意事项：**
- 只能启动一次，重复按 `s` 会提示已启用
- 启动前确保小车周围安全，有足够的活动空间

---

### 🛑 强制停止 [p]

**功能：** 立即停止小车并关闭自动控制

**使用场景：**
- 紧急情况需要立即停车
- 发现异常行为需要中断
- 测试完成需要停止

**执行操作：**
1. 立即发送 `CMD-SET 0 0` 停止电机
2. 关闭自动控制标志
3. 返回"仅建图模式"

**状态提示：**
```
[Visualizer] 🛑 强制停止！小车将立即停止并关闭自动控制。
```

**停止后：**
- 可以再次按 `s` 重新启动
- 建图和可视化继续运行

---

### 💾 保存地图 [m]

**功能：** 保存当前地图和轨迹到文件

**生成文件：**
- `map.png` - 当前SLAM地图的图像
- `path.csv` - 机器人行驶轨迹的CSV文件

**文件格式：**

`map.png`：
- PNG格式的地图图像
- 包含：占据栅格、机器人位置、激光点云、前沿区域等
- 可用于后续分析或展示

`path.csv`：
```csv
x,y
2.000,2.000
2.050,2.003
2.098,2.015
...
```

**使用场景：**
- 实验结束保存数据
- 记录探索过程
- 生成报告素材
- 对比不同算法效果

**状态提示：**
```
[Visualizer] 💾 地图已保存至 map.png, 路径已保存至 path.csv
```

**注意事项：**
- 可以随时保存，不影响运行
- 多次保存会覆盖之前的文件
- 建议在关键节点或实验结束时保存

---

### ⏸️ 暂停可视化 [空格]

**功能：** 暂停或继续matplotlib窗口的绘制更新

**重要说明：** 
⚠️ 仅暂停可视化，**控制循环继续运行**！小车仍在移动！

**用途：**
- 降低CPU/GPU占用（可视化渲染很耗资源）
- 长时间运行时减少界面卡顿
- 专注于终端输出的调试信息

**状态切换：**
- 第一次按空格：
  ```
  [Visualizer] ⏸️  可视化已暂停（控制循环继续运行）
  ```
- 再次按空格：
  ```
  [Visualizer] ▶️  可视化已继续
  ```

**暂停期间：**
- ✅ 小车继续移动和探索
- ✅ SLAM继续建图
- ✅ DWA继续规划路径
- ❌ matplotlib窗口不更新
- ❌ 无法看到实时状态

**适用场景：**
- 长时间运行（几小时）
- 远程SSH连接（减少网络传输）
- 性能受限的机器
- 只需要最终结果，不需要观看过程

**恢复可视化：**
- 再次按空格键
- 窗口立即显示最新状态
- 期间的数据不会丢失

---

## 典型使用流程

### 场景1：完整探索流程

```
1. 启动程序
   → 窗口显示初始地图
   
2. 按 [s] 启动自动控制
   → 小车开始移动
   → 观察建图效果
   
3. 发现问题，按 [p] 停止
   → 检查原因
   → 调整参数
   
4. 再次按 [s] 继续
   → 继续探索
   
5. 完成后按 [m] 保存
   → 生成 map.png 和 path.csv
```

### 场景2：性能优化运行

```
1. 启动程序并按 [s]
   → 开始探索
   
2. 确认运行正常后按 [空格]
   → 暂停可视化
   → CPU占用下降
   
3. 长时间运行（通过终端监控）
   
4. 完成前按 [空格] 恢复可视化
   → 查看最终地图
   
5. 按 [m] 保存结果
```

### 场景3：调试模式

```
1. 启动程序
   → 观察初始状态
   
2. 按 [s] 启动
   → 观察几秒
   
3. 按 [p] 停止
   → 检查终端输出
   → 分析问题
   
4. 重复 2-3 步骤
   → 逐步调试
   
5. 确认无误后完整运行
```

## 常见问题

### Q: 按了 `s` 但小车没动？

**可能原因：**
1. 环境变量未设置：检查 `ENABLE_CONTROL_LOOP=1`
2. BLE未连接：查看终端是否有 `[BLE] 连接成功` 提示
3. 电机参数问题：检查 `MIN_ENCODER_SPEED` 和 `MOTOR_SPEED_SCALE`

**排查步骤：**
```bash
# 检查环境变量
echo $ENABLE_CONTROL_LOOP
echo $USE_REAL_BLE_DATA

# 查看终端输出
# 应该看到 [DWA] v=... w=... → CMD-SET ... ...
```

### Q: 按了 `p` 但小车还在动？

**可能原因：**
1. 命令延迟：BLE通信有轻微延迟（<100ms）
2. 惯性：小车需要制动时间
3. 命令未发送：检查 BLE 写入特征是否正确

**正常情况：**
- 1-2秒内完全停止
- 如果超过3秒仍在动，说明有问题

### Q: `m` 键保存后文件在哪？

**位置：** 运行 `python main.py` 的当前目录

**查找方法：**
```bash
# 在工作目录下
ls -lt map.png path.csv

# 完整路径
pwd
# 输出：/Users/nemoyu/Desktop/小学期/gpt_code_gpu
```

### Q: 空格暂停后怎么知道小车在动？

**监控方法：**
1. 观察终端输出的 DWA 速度命令
2. 监听小车的声音（电机声）
3. 使用其他设备观察（摄像头、另一台电脑）

**终端输出示例：**
```
[DWA] v=0.35 w=0.42 → CMD-SET 54 76
[SLAM] ICP iterations: 15, error: 0.032
[探索] 当前前沿: 23 个，目标: (3.2, 4.5)
```

## 技术细节

### 按键事件处理

**实现位置：** `visualizer.py` → `Visualizer._on_key_press()`

**事件绑定：**
```python
self.fig.canvas.mpl_connect('key_press_event', self._on_key_press)
```

**状态标志：**
- `auto_control_enabled`: 自动控制是否启用（s键控制）
- `force_stop_requested`: 强制停止请求（p键触发）
- `paused`: 可视化暂停状态（空格键切换）

### 与主循环的交互

**主循环检查：**
```python
# 在 main.py 的 apply_motion_update() 中
if viz.check_and_clear_force_stop():
    ble_bridge.send_motor_command(0.0, 0.0, dt)
elif ENABLE_CONTROL_LOOP and viz.is_auto_control_enabled():
    ble_bridge.send_motor_command(v_cmd, w_cmd, dt)
```

**调用关系：**
```
用户按键 → matplotlib事件 → Visualizer状态更新 
→ 主循环查询状态 → 决定是否发送命令
```

## 快捷键记忆技巧

| 键 | 助记 | 英文 |
|----|------|------|
| s | **S**tart | 开始 |
| p | sto**P** / **P**ause | 停止 |
| m | **M**ap | 地图 |
| 空格 | 通用暂停键 | Pause |

## 相关文档

- `visualizer.py` - 可视化模块实现
- `main.py` - 主循环和控制逻辑
- `slam_drift_fix.md` - SLAM漂移修复说明
- `turning_speed_fix.md` - 转弯速度优化说明
